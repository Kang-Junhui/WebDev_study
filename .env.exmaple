# Django Board Project 환경변수 설정 예시
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요

# === 데이터베이스 설정 ===
# PostgreSQL 데이터베이스 연결 정보
DB_NAME=board_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# === Django 서버 설정 ===
# Django 개발 서버 설정
DJANGO_HOST=0.0.0.0
DJANGO_PORT=5008

# === 외부 서버 접속용 설정 ===
# 다른 사람의 서버에 접속할 때 사용
# DB_HOST=192.168.0.49
# DB_USER=test_reader
# DB_PASSWORD=reader123

# === Windows 사용자 참고 ===
# Windows에서 로컬 PostgreSQL 사용 시:
# DB_HOST=localhost
# DB_USER=postgres
# DB_PASSWORD=your_local_password

# === 협업 테스트용 설정 ===
# 공용 테스트 서버 접속 시:
# DB_HOST=192.168.0.49
# DB_USER=test_reader
# DB_PASSWORD=reader123
# DJANGO_PORT=8000  # 충돌 방지를 위해 다른 포트 사용
